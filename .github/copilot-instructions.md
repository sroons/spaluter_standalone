# Copilot Instructions for This Repository

## Build, test, and lint commands

This repository is currently empty (no source files or build/test/lint tooling is configured yet).

- Build: not configured
- Test: not configured
- Lint: not configured
- Single-test execution: not configured

When tooling is added, prefer commands documented in `README.md` or project config files (`package.json`, `pyproject.toml`, `Makefile`, etc.) and update this file with exact commands.

## High-level architecture

There is no application architecture yet. Establish this section once the first project structure is added (for example: app/runtime boundaries, major modules, and data flow between them).

## Key conventions

No repository-specific coding conventions are defined yet in tracked docs or config files.

As conventions are introduced, capture only project-specific patterns here (for example: module layout, naming patterns tied to domain concepts, test organization, or config layering rules).
